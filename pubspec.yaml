name: my_flutter_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.0
#  flutter_plugin_v1:
#    git:
#      url: git@github.com:Odialko/plugin_v1.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.8.0
  grinder: ^0.8.6
  intl_translation: ^0.17.9
  intl: ^0.16.0
  intl_utils: ^1.3.0
  archive: ^2.0.11

flutter:
  uses-material-design: true


flutter_intl:
  enabled: true
  class_name: S
#  main_locale: uk_UA # Optional. Sets the main locale used for generating localization files. Provided value should comply with ISO-639-1 and ISO-3166-1 (e.g. "en", "en_GB"). Default: en
